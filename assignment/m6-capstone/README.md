# M6 capstone: data-driven pipeline

This folder holds the capstone for the Spark-style milestone: one shared dataset, a written sequence of operations you must implement yourself, and a reference `expected.json` so you can self-check your semantics (not your source code layout).

There is no official solution script you are required to run. Different submissions will structure callbacks, helpers, and fluent versus imperative calls differently. Matching `expected.json` after the canonicalization step below means your outputs match the specification.

## Files

| File | Role |
|------|------|
| `data.json` | Records to load into the store, plus key lists for the wide operations. |
| `expected.json` | Canonical reference output for the operation sequence (regenerated by course staff from the reference implementation). |
| `reference-pipeline.js` | Instructor-only reference used to regenerate `expected.json` (same semantics as this README). |

## Setup

1. Parse `data.json`. For each object in `records`, insert the value into your group store under `key` (same pattern as other milestones: `put(value, key)` or your API’s equivalent).
2. Let `allKeys` be the list of every `key` in `records`, sorted in ascending string order (so the pipeline input order is fixed for grading).

## Operation sequence (exact semantics)

Perform these steps in order. Use your Spark-style API’s lazy fluent chain for step 1 and actions / imperative methods where needed for steps 2–3.

### 1. Fluent chain on `allKeys`, then collect

Build a pipeline from `allKeys` with:

1. filter — Keep a `(key, value)` pair only if `value` is a string and does not contain the substring `SKIP` (case-sensitive, as in JavaScript `String.prototype.includes`).
2. map — Replace the value with `String(value).trim().toUpperCase()`; emit a single object `{ [key]: newValue }` per input (same key as in the store).
3. flatMap — For each `(key, value)` after the map, emit two identical objects: `[{ [key]: value }, { [key]: value }]`.
4. collect — Run the action and collect the resulting array of single-key objects. Call this array fluentCollect when comparing to `expected.json`.

### 2. sortByKey

Call `sortByKey` on the key list `keysForSort` from `data.json`, with ascending key order (`ascending: true` or your API’s equivalent). Call the result sortByKey for comparison.

### 3. join

Call `join` (inner join) with `keysJoinA` and `keysJoinB` from `data.json`. Call the result join for comparison.

## Comparing to `expected.json`

MR merge order can permute rows that are logically the same set. Before comparing, canonicalize each of the three arrays (`fluentCollect`, `sortByKey`, `join`):

- Sort the array of objects with a stable comparator: for each object, sort by `Object.keys(row)[0]`, then by `JSON.stringify(row[thatKey])`, so duplicate keys and nested values compare consistently.

After canonicalization, your three arrays should deep-equal the three arrays in `expected.json`.

You may use `node m6/check-capstone.js <your-output.json>` if your output file has the shape `{ "fluentCollect": [...], "sortByKey": [...], "join": [...] }` (same keys as `expected.json`).

## Instructor note

Regenerate `expected.json` after changing `data.json` or the published semantics:

```bash
./scripts/kill-ports.sh
node m6/generate-capstone-expected.js
```

The generator uses ports `2150–2152` for workers (listed in `scripts/kill-ports.sh`).
